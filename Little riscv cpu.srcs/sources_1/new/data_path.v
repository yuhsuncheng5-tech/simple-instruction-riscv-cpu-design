module data_path(input clk, input reset);
// pc

// IF/ID
always @(posedge clk)begin
end

// ID/EX
always @(posedge clk)begin
end

// EX/MEM
always @(posedge clk)begin
end

// MEM/WB
always @(posedge clk)begin
end

endmodule